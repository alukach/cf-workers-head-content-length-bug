{
  "name": "cf-parquet-stream-poc",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker POC that streams a parquet file (GET) and reports its size (HEAD)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "wrangler": "^4.20.0",
    "typescript": "^5.5.0"
  }
}
